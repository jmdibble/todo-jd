import React from 'react';

import ListItem from './ListItem';

const List = props => {
  return (
    <div>
      <ListItem />
      <ListItem />
      <ListItem />
    </div>
  );
};

export default List;
